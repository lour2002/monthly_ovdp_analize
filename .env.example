# Copy to .env and fill in real values. NEVER commit .env to git.

# Bearer token for Anthropic Claude Code routine
ANTHROPIC_ROUTINE_TOKEN=sk-ant-oat01-...

# Notion Integration Token (optional — used if routine writes to Notion directly)
NOTION_TOKEN=secret_...
